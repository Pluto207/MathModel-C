# 数模 C 使用指南

## 选择模式

### 训练模式
完整执行 Stage 0–9，启用模型门、最小运行门、结果证据门、论文门，并可增加独立 Agent 复核、参数扫描、MATLAB/Python 对照和反方审稿。

### 比赛模式
只保留：选题 → 模型合同 → MATLAB 最小切片 → 全量求解 → 结果锁定 → 论文 → 终审。每问先保底，再扩展；不为满足图数或文档数量制作无效产物。

## 三人分工

1. 建模主：写题意、变量、假设、方程、目标函数、约束和验证设计。
2. MATLAB 主：实现 `main_qX.m`，保存结果和图，维护运行日志与复现清单。
3. 论文主：维护符号和主张—证据表，装配 `论文/论文.tex`，做格式与匿名审查。

主责不等于隔离：核心模型由 MATLAB 主检查可实现性，核心代码由建模主检查语义，摘要和最终数字由全员复核。

## 推荐启动提示

```text
进入数模C比赛模式。目标：CUMCM 2026，队伍三人，MATLAB为主、AI/Agent为辅助。
先读取题目和全部附件，输出每问的输入、输出、约束、评价指标、数据风险、物理机制和依赖图。
不要先写摘要或结论；先建立求解计划和模型合同。每问先做可解释基线，再决定是否引入复杂算法。
所有代码从项目根目录运行，结果保存到结果/，图保存到图表/，并维护主张—证据映射和AI使用台账。
```

## 推荐项目结构

```text
项目/
├── state/decision_log.json
├── 题目/
├── 数据/
├── 求解/问题1/…
├── 结果/
├── 图表/
├── 论文/
└── 支撑材料/
```

## 结果状态

- `DRAFT`：AI或队员草稿，不能直接进论文。
- `REVIEWED`：至少一名队员已核对。
- `APPROVED`：涉及核心假设、目标、约束、关键数字和摘要时由两名队员确认。

只有 `APPROVED` 内容才能进入终稿。
