# 复现与版本固化

每个问题应保留一份复现清单，并将它与最终结果绑定。建议记录：

```text
模型版本、MATLAB版本、工具箱、输入文件和摘要、输入SHA-256、主入口、运行命令、随机种子、参数、输出、耗时、验证状态和已知风险。
```

MATLAB 结果是核心结果唯一权威来源。若使用 Python 做格式处理，必须说明其不改变核心数值或图形语义。

结果发生实质变化后，复现清单、主张—证据表和论文摘要数字都退回 REVIEW/DRAFT，不能沿用旧状态。
