# 数模 C — 队员部署说明

> 给队友的 5 分钟上手指南。遇到任何问题先看文末「常见问题」。

---

## 一、你需要的环境

| 软件 | 要求 | 没有会怎样 |
|---|---|---|
| Git | 任意版本 | 无法克隆仓库 |
| Python | 3.10+ | CLI 工具不可用 |
| MATLAB | R2020+ | 无法跑主求解（建模主核心工具） |
| XeLaTeX | TeX Live 2023+ | 无法编译中文论文（论文主核心工具） |
| pandoc 或 wkhtmltopdf | 可选 | 仅 AI 详情 PDF 需要，没有就生成 Markdown |

> ⚠️ 三人至少保证：**会写代码的人有 MATLAB**、**负责论文的人有 XeLaTeX**。Windows 上装 TeX Live 或用 VS Code + LaTeX Workshop 均可。

---

## 二、克隆与部署（Windows 示例，Mac/Linux 命令相同）

打开终端（Win 用 PowerShell），执行：

```bash
# 1. 克隆仓库（放哪都行，建议放一个专门的建模文件夹）
git clone https://github.com/Pluto207/MathModel-C.git
cd MathModel-C

# 2. 准备字体（必须！否则中文论文编译失败）
#    思源宋体（OFL 协议，需联网下载一次，约 40MB）
python tools/fonts/fetch_sourcehan.py

#    Windows 系统字体（Times/Arial/楷体/Consolas，从你电脑自动复制）
python tools/fonts/setup_fonts.py

# 3. 自检（应输出 status: PASS）
python tools/c.py self-check
```

看到 `status: PASS` 即部署成功。

---

## 三、三台电脑和不同 Agent 怎么协作

- 每台电脑各自克隆并安装 Skill；OpenClaw、Claude Code、Codex 或其他 Agent 互不影响。
- 比赛项目不要上传 GitHub。在线下集中时使用可信局域网共享目录；临时分开时按 `docs/多机异构Agent交接规范.md` 使用受控离线交接包。
- 队长指定一个唯一权威项目目录；不要三台电脑各自维护“最终版”。
- 同一时刻只能一个人写同一文件：论文由论文主写，`state/decision_log.json` 由队长/整合负责人写，结果由对应 MATLAB 主生成。
- 完整的 A 初稿→C 优化分工见 `docs/A初稿到C优化整合流程.md` 和 `docs/三人分工与Skill使用方案.md`。

## 三、比赛当天怎么用

### 第一步：队长初始化题目

```bash
python tools/c.py init 2026国赛-A题 --questions 4
```

生成项目目录：

```text
2026国赛-A题/
├── state/          # 状态与决策记录（队长维护）
├── 题目/           # 赛题 PDF 放这里
├── 数据/           # 附件数据放这里
├── 求解/问题1~4/   # 每问代码 + 每问《模型合同》
├── 结果/           # MATLAB 输出（CSV/MAT/JSON）
├── 图表/           # 正式图表
├── 论文/           # 中文 LaTeX 论文（已含模板，可直接编译）
└── 支撑材料/       # 提交用支撑文件（含 AI 使用详情）
```

### 第二步：三人分工（见根目录 `团队分工.md`）

| 角色 | 干什么 | 交付物 |
|---|---|---|
| 🧠 建模主 | 拆题、建模、写假设 | 每问《模型合同.md》 |
| 💻 MATLAB 主 | 写代码、跑结果、画图 | 结果文件 + 图表 |
| ✍️ 论文主 | 写论文、核数字、编译 | `论文/论文.tex` → PDF |

### 第三步：常用命令速查

```bash
python tools/c.py check    <项目目录>   # 目录/状态检查
python tools/c.py evidence <项目目录>   # 主张—证据表检查
python tools/c.py version  <项目目录>   # 结果版本检查（改动自动打回）
python tools/c.py figure   <项目目录>   # 图表契约检查
python tools/c.py paper    <项目目录>   # 论文占位符检查
python tools/c.py ai-report <项目目录>  # 生成 AI 使用记录
```

### 第四步：MATLAB 求解（编程手）

每个问题都有入口模板 `求解/问题X/main_qX.m`，先跑通最小例子再扩展。工具函数在 `tools/matlab/`：

```matlab
addpath(genpath('数模C所在路径/tools/matlab'));   % 一次性
check_matlab_env();          % 检查环境
save_result_bundle(...);     % 结果固化（自动生成 manifest）
```

---

## 四、和队友协作（重要！）

- **共享同一份代码库**：建议 Git 分支协作，队长是 `main` 保护者；每完成一问 commit 一次，写清提交信息。
- **谁改了什么要说**：改模型/参数/结果后，运行 `version` 检查——它会自动把受影响的论文主张打回"待复核"。
- **AI 用法**：AI/Agent 可以写代码、做审查、润色论文，但**核心建模决策是你们仨的**。每次用 AI 记得记录到 `AI使用台账.json`（比赛规则要求披露）。
- **文件命名**：代码 `snake_case`，结果带日期或版本号，别覆盖队友的文件。

---

## 五、常见问题

**Q1: `fetch_sourcehan.py` 下载失败？**
网络问题，多试几次，或从 https://github.com/adobe-fonts/source-han-serif/releases 手动下载 SourceHanSerifSC Regular/Bold 放到 `templates/cumcm/论文/fonts/`，改名为 `SourceHanSerifCN-Regular.otf` / `SourceHanSerifCN-Bold.otf`。

**Q2: `setup_fonts.py` 说找不到 Windows 字体？**
确认你确实在 Windows 上，且 `C:\Windows\Fonts` 存在。若在 macOS/Linux，需手动提供等效字体（README 有说明），或找一位 Windows 队友生成后打包传给你——**注意：这些字体不可再分发，只限队伍内部使用**。

**Q3: `c.py` 报 python 找不到？**
确认 `python --version` ≥ 3.10；Windows 有时要用 `py` 代替 `python`。

**Q4: 论文编译报字体错误？**
大概率是第 2 步字体没跑成功。重新执行 fetch + setup，再 `cd 论文 && xelatex 论文.tex`。

**Q5: 我需要学完整个 Skill 才能用吗？**
不需要。**先跑通 `init` + 一题 MATLAB + 论文模板编译**就够上场。其余（Stage 文档、证据链、方法论）边用边查。

---

## 六、底线提醒

1. **竞赛规则以当届官方为准**——本 Skill 只是工具，不替代规则原文。
2. 论文必须**人写人核**，AI 只是助手；关键数字、模型、结论由队伍负责。
3. 交卷前跑一遍 `CUMCM终审清单.md`（项目根目录有），逐项勾选。
4. 赛题和附件是保密的，**不要**把题目数据传到公开仓库或外部 AI 服务。

祝我们建模顺利，一起拿奖 🏆
