# P4–P6 后续增强工具说明

- `tools/matlab/save_result_bundle.m` 现同时输出 `.mat`、`.txt`、`.json`，并记录 `model_version` 与 `result_version`。
- `tools/version_check.py` 首次建立结果 manifest 快照；后续检测到 manifest 改变时把状态置为 `REVIEW`。需要实际改写证据表时显式加 `--apply`；默认只报告并更新快照。
- `tools/matlab/result_to_latex.py` 将 MATLAB 导出的 CSV/JSON 转为可人工审核的 LaTeX 表格片段，不自动覆盖论文。
- AI 详情 PDF 入口已在 `tools/ai-agents/build_ai_report.py --pdf`；当前环境若无 pandoc/wkhtmltopdf，仍会诚实返回 BLOCKED 并保留 Markdown。
