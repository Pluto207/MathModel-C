# 第二阶段 P4–P6

## P4 证据链
`tools/evidence_check.py` 只读检查主张—证据表：状态词、主张、证据单元格、反引号路径、审核人，并输出 `结果/evidence_check.json`。

## P5 AI台账
`tools/ai-agents/record_ai_use.py` 追加一次不含秘密的使用记录；`build_ai_report.py` 从台账生成 `支撑材料/AI工具使用详情.md`。不要记录密钥、完整私人对话或竞赛禁止外传的题面。

## P6 Agent路由
`route_task.py` 生成有明确读/写/禁止范围的任务 JSON；`task_router.md` 规定角色顺序和返回格式。工具不自动启动 Agent，不突破项目写入边界。
