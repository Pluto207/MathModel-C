# 第二阶段增强说明

## 已实现

- `tools/init_project.py`：一键生成 CUMCM 三人项目目录、状态文件、模板和每问 MATLAB 入口。
- `tools/project_check.py`：只读检查实际项目结构和状态 JSON。
- `tools/evidence_check.py`：只读检查主张—证据表中的代码/结果/图表路径。
- `tools/matlab/check_matlab_env.m`：MATLAB 版本、基础函数和工具箱许可预检。
- `tools/matlab/save_result_bundle.m`：保存结果、验证状态、版本、种子和运行时间摘要。
- `tools/ai-agents/README.md`：Agent 路由、互斥写入和统一返回格式。

## 边界

初始化工具只写用户指定的新项目目录；C 根目录只存 Skill 和模板。所有科学结论仍需队员审核。A/B 为只读参考源。
